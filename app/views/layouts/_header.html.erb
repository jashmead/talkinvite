<div data-role="header">
  <% if signed_in? %>
    <a href="<%=root_path%>">Home</a>
    <h1><%=yield :title%></h1>
    <a href="<%=settings_path%>">Settings</a>
  <% else %>
    <a href="<%=signin_path%>">Log In</a>
    <h1><%=yield :title%></h1>
    <a href="<%=signup_path%>">Sign Up</a>
  <% end %>
</div>
