<!-- cloned from people/found.html.erb -->
<!-- without the 'as:', the talk variablae does not show up inside 'talk_found' -->

<% provide(:title, 'Talks') %>

<%= will_paginate %>
<ul class="talks">
  <%= render partial: 'talk_found', collection: @talks, as: :talk %>
</ul>
<%= will_paginate %>

<br>

<%= link_to 'New Talk', new_talk_path %>
