<!-- could evolve this into a generalized talks footer -->
<% logger.debug("VV: _start_talk_button") %>
<%= button_to( 'Start Talk', { action: 'new' }, { method: 'get', form_class: 'btn btn-primary btn-mini' }) %>
