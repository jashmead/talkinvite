<% provide(:title, 'Talk') %>
<% 
  # title:  summary of talk, maybe change color if you are an admin
  # content:  name, description, when, where, partners, comments
  # footer: new talk, search talks, messages
  # admin/originator:  show timeline, allow edits on name, description, posts
  # join/leave button
%>
<%= render 'form' %>
