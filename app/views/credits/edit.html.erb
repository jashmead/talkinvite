<% content_for(:title, 'Thank ' + @credit.name) %>

<%= render 'form' %>

<% if admin? %>
<%= button 'Delete', @credit, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>

<%= button_back credits_path %>
