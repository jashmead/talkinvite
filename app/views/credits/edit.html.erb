<% content_for(:title, 'Thank ' + @credit.name) %>

<%= render 'form' %>

<% if admin? %>
<%= link_to 'Unthank', @credit, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>

<%= link_to 'Back', credits_path %>
