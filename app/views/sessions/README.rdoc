=== Sessions

-- all of the forms here should be dialogs

==== Signin

-- should allow both name & email signin

-- includes reset password button

-- includes new account button

==== Signout

==== TBD: Reset Password

needs to be a form so they can specify the name and/or the email to send the new password to
-- put the supporting fields in the session object?
-- see the examples before moving further on this

==== Signout Button

very prominent, a security plus
