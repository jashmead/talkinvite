<% 
  model = submit_button.object
  name = model.class.to_s
  if submit_button.object.new_record?
%>
  <%= submit_button.submit 'Create ' + name %>
<% else %>
  <%= submit_button.submit 'Update ' + name %>
  <%= button_delete model %>
<% end %>
