<% provide(:title, "Change Password") %>
<% #logger.debug("views/people/change_password: @title: #{@title}, @person: #{@person.inspect}") %>
<%= form_for_jqm(@person) do |f| %>

  <%= render partial: 'shared/error_messages', object: f.object %>

  <% # TBD:  verify previous password first; change submit button to read 'Change Password'; setup better specs %>
  <%= render partial: 'password', object: f %>

  <%= render partial: 'person_save', object: f %>

<% end %>
