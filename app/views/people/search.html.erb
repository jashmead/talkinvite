<!-- search form for people
  -- started with 'ransack', but that gave mysterious failures
  -- switch to a straight search + index switch approach
    -- use hidden field tag to drive the switch
-->
<% provide(:heading, 'Search for people') %>
<% provide(:title, 'Search for people') %>
<h1><%= yield :heading %></h1>

<div class="row">
  <div class="span6 offset3">

    <%= form_tag("/people/found", method: "get") do %>
      <%= label_tag(:search_q, "Search for:") %>
      <%= search_field(:search, :q) %>
      <%= submit_tag("Search") %>
    <% end %>

  </div>
</div>
