<% provide(:title, 'Sign up') %>
<% provide(:data_role, 'dialog') %>
<% provide(:data_role, 'dialog') %>

<%= form_for(@person) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render 'fields', f: f %>
  <%= f.submit "Create my account", 'data-inline' => true %>
<% end %>
