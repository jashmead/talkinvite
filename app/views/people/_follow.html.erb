<%= form_for(current_person.relationships.build(followed_id: @person.id), remote: true) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Follow", 'data-inline' => true %>
<% end %>
