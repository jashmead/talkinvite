<% provide(:title, "Home") %>

<% if @talks %>
  <%= render 'talk', collection: @talks %>
<% end %>

<%= link_to 'Create Talk', new_talk_path(@person), 'data-role' => 'button', 'data-inline' => true %>
